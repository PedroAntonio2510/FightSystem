spring:
  application:
    name: FightSystem
  h2:
    console:
      enabled: true
      path: /h2-console
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:testdb
    password: sa
    username: sa

  jpa:
    show-sql: true
  flyway:
    enabled: true

jwt:
  secret: minha-secret-aqui